name: Novo site

on:
  pull_request:
    branches:
      - main

jobs:
  analisar:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Novo site 
        