name: Build Docker novo-site

on:
  push:
    branches:
      - main
    # paths:
    #   - 'novo-site/index.html'
    #   - 'novo-site/style.css'
    #   - 'novo-site/script.js'
    #   - 'novo-site/Dockerfile'
    #   - '.github/workflows/build-docker-novo-site.yaml'
  workflow_dispatch:
    inputs:
      ambiente:
        description: 'Ambiente de build (dev, staging, prod)'
        required: true
        default: 'dev'
        type: choice
        options:
          - dev
          - staging
          - prod

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Acessar EC2
        run: |
          echo "Joining EC2 instance to the workflow..."
          # Comandos para conectar à instância EC2, se necessário
          pwd
          chmod 400 "docker-vm-key.pem"
          ssh -i "docker-vm-key.pem" ubuntu@ec2-18-116-118-185.us-east-2.compute.amazonaws.com

      - name: Testar vm
        run: |
          echo "Testando conexão com a VM..."
          pwd   

      # - name: Checkout repository
      #   uses: actions/checkout@v4

      # - name: Set up Docker Buildx
      #   uses: docker/setup-buildx-action@v3

      # - name: Gerar tag única
      #   id: tag
      #   run: |
      #     AMBIENTE="${{ github.event.inputs.ambiente || 'dev' }}"
      #     TAG="${AMBIENTE}-$(date +%Y%m%d%H%M%S)-${GITHUB_SHA}"
      #     echo "tag=$TAG" >> $GITHUB_OUTPUT
      #     echo "image=meu-novo-site:$TAG" >> $GITHUB_OUTPUT

      # - name: Build novo-site
      #   run: |
      #     cd novo-site
      #     docker build -t ${{ steps.tag.outputs.image }} .
      #     echo "Imagem do novo-site buildada!"

